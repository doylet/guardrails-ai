# Copilot: Demos on Rails
Generate YAML scenarios in `demo_scenarios/` and run with:
`python tools/demo_harness.py run demo_scenarios/<name>.yaml`
Do not create ad-hoc demo.py scripts that import internals.
